<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.repository.session.CommentSessionRepository">
    <select id="selectComment" parameterType="long" resultType="org.example.model.Comment">
       SELECT
           id AS id,
           user_id AS userId,
           content AS content,
           reg_date AS regDate
        FROM COMMENT
        WHERE id = #{id}
    </select>

    <insert id="insertComment" parameterType="Comment">
        INSERT INTO COMMENT(id, user_id, content, reg_date)
            VALUES (#{id}, #{userId}, #{content}, #{regDate})
    </insert>

    <update id="updateComment" parameterType="Comment">
        UPDATE comment
            SET content = #{content}
        WHERE id = #{id}
    </update>

    <delete id="deleteComment" parameterType="long">
        DELETE FROM comment
        WHERE id = #{id}
    </delete>
</mapper>